# Default dependencies — pure-Python only. The optional native Leiden backend
# lives in requirements-native.txt (see it for the backend-selection note).
networkx>=3.2
requests>=2.31.0
beautifulsoup4>=4.12.0
# Weekly briefing email rendering (tools/_briefing/render.py) — markdown → HTML.
# smtplib/email are in the standard library, so no extra dependency is needed.
mistune>=3.0
# Regression tests (tests/) — `python -m pytest tests/`.
pytest>=8.0
