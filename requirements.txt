networkx>=3.2
requests>=2.31.0
beautifulsoup4>=4.12.0
# Leiden community detection (tools/_build/clusters.py) — igraph + leidenalg.
# OPTIONAL since 2026-08: without them the build falls back to the pure-Python
# Leiden in tools/_build/_leiden_py.py (same RBConfiguration quality, verified
# to reproduce the C output on the shipped corpus). Install for large corpora
# where the C implementation is much faster.
igraph>=0.11
leidenalg>=0.10
# Weekly briefing email rendering (tools/_briefing/render.py) — markdown → HTML.
# smtplib/email are in the standard library, so no extra dependency is needed.
mistune>=3.0
# Regression tests (tests/) — `python -m pytest tests/`.
pytest>=8.0
